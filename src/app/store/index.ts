// src/app/store/index.ts
// Root store configuration

// Re-export feature stores for composition
export { useAuthStore } from '../../features/auth';

// Future: Add root store composition here if needed

